1.Customer:name,身份证，入住时间，入住天数，构造方法和toString
2.Room:房间编号，房间客户，是否空闲，方法：入住，退房
3.Hotel:楼层数，每层房间数，Room[][]数组，（方法）功能：1.查询所有空房，
	2.可根据房间编号查询房间的信息，
4.Manager:可查询某个房间，可查询所有空房，可入住，可退房

/*
把字符数组ch中的大写字符转为小写，小写转为大写，如果发现数组型字符，则把数字转
为对应的负数输出‘5’
*/