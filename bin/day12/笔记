final关键字：
	1.修饰类:如果用来修饰一个类，则该类不可被继承
	2.修饰方法：那么该方法不可被重写
	3.修饰属性：那么该属性则是常量，只有一次赋值机会,可在三个地方赋值：
		a.在定义时被 初始化.
		b.在代码块中赋值,对于静态属性，可在静态代码块赋值，不可在普通代码中赋值
		c.在构造方法中初始化
	4.修饰局部变量:只有一次赋值机会，被final修饰的形参在函数内部是只读

包装类：
	Integer i = 3;//自动装箱
	int a = i;//自动拆箱
	
字符串与基本类型的互换：
	String s = 10+"";
	int a = Integer.parseInt(s);
	
	/*85，78，98
	从键盘一次性输入三门课的成绩，用,号隔开，要求取出三个数字，并转为基本数据类型
	*/
		