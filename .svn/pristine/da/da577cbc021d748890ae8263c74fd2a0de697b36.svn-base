package day16.克隆;
/*实现克隆的步骤：
 * 1.实现接口Cloneable
 * 2.重写方法clone();
 * 
 * 注意：对于引用数据类型的属性，只能克隆地址，不能把该属性的对象也克隆一份新的出来
 * */
public class Dog implements Cloneable{
	StringBuffer name;
	int age;
	protected Object clone() throws CloneNotSupportedException
	{
		return super.clone();
	}
}
